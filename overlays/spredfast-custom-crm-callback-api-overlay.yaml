overlay: 1.0.0
info:
  title: API Evangelist enhancements — Spredfast Custom CRM Callback Contract
  version: 1.0.0
extends: ../openapi/spredfast-custom-crm-callback-api-openapi.yml
x-generated: '2026-08-13'
x-method: generated
x-source: openapi/spredfast-custom-crm-callback-api-openapi.yml + developer.khoros.com/khorosmarketingdevdocs
x-note: 'API Evangelist overlay. Captures our enhancements to the harvested contract WITHOUT mutating
  it: a descriptive title and description the provider omitted (the source documents carry a bare lowercase
  filename as info.title and no description), contact and licence pointers, and the runtime semantics
  documented in prose but absent from the contract. Nothing here is invented — every value is sourced
  from the Khoros Marketing developer center or from a sibling artifact in this repo.'
actions:
- target: $.info
  description: Give the document a real title and description; the source names it "api-specifications".
  update:
    title: Spredfast Custom CRM Callback Contract
    description: 'The INVERTED contract: the customer implements this server on their own domain and Khoros
      Marketing calls it. Cases, customers and object metadata.'
    contact:
      name: Khoros Marketing Developer Center
      url: https://developer.khoros.com/khorosmarketingdevdocs
    x-provider: spredfast
    x-successor: khoros
    x-status-page: https://status.khoros.com
- target: $
  description: Record the cross-cutting runtime semantics the contract does not declare.
  update:
    x-idempotency:
      supported: false
      note: No idempotency key of any kind; retrying a publish POST publishes twice.
    x-rate-limits:
      published: false
      headers: []
      note: No published limit and no rate-limit response header on any operation.
    x-error-format:
      rfc9457: false
      envelope: '{ "status": { "error": { "code", "message" }, "succeeded": false } }'
      artifact: errors/spredfast-problem-types.yml
    x-conventions: conventions/spredfast-conventions.yml
    x-lifecycle: lifecycle/spredfast-lifecycle.yml
    x-authentication: authentication/spredfast-authentication.yml
    x-harvest:
      method: extracted from the rendered ReadMe reference pages at developer.khoros.com/khorosmarketingdevdocs;
        the raw definition endpoint returns HTTP 403 anonymously and every static spec path returns the
        docs SPA HTML shell
      date: '2026-08-13'
- target: $.servers
  description: 'Explain the templated host: this contract is implemented by the CUSTOMER, not by Spredfast.'
  update:
  - url: https://{your-domain}.com
    description: 'Customer-implemented. Khoros Marketing is the CLIENT of this contract: the customer
      stands the server up on their own domain and registers it via POST /admin/custom on the Spredfast
      CRM API. The templated host is correct, not a defect.'
    variables:
      your-domain:
        default: your-domain
